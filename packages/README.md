# packages/

Vacío por ahora. Reservado para código compartido entre `apps/frontend` y `apps/backoffice` (tipos, cliente de API, componentes UI) el día que duplicarlo entre las dos deje de ser sostenible — ver la nota de deuda en [../docs/arquitectura.md](../docs/arquitectura.md#deuda-de-arquitectura-conocida).

Migrar algo acá hoy requiere pnpm workspaces (o similar); las tres apps siguen instalándose de forma independiente con npm. No crear nada en esta carpeta sin resolver antes esa migración.
